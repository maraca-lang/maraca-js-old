{
  "name": "tabla",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf lib/* && nearleyc src/grammar.ne -o lib/grammar.js && tsc",
    "test": "yarn build && ts-node src/test.ts"
  },
  "devDependencies": {
    "moo": "^0.4.3",
    "nearley": "^2.15.1",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  }
}
