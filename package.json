{
  "name": "maraca",
  "version": "0.2.6",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "build": "rimraf lib/* && nearleyc src/parse/grammar.ne -o lib/grammar.js && tsc",
    "prepare": "yarn build",
    "test": "yarn build && node lib/test/index.js"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "moo": "^0.5.0",
    "nearley": "^2.16.0"
  },
  "devDependencies": {
    "prettier": "^1.16.3",
    "rimraf": "^2.6.3",
    "typescript": "^3.3.1"
  }
}
